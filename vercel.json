{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "pnpm -w run typecheck:libs && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/empathiq run build && echo \"DIAG_PWD=$(pwd)\" && echo DIAG_LS_CWD: && ls -la && echo DIAG_FIND_APPHTML: && find / -name app.html -path '*dist/public*' -not -path '*/node_modules/*' 2>/dev/null && rm -rf public && cp -r artifacts/empathiq/dist/public public",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/app", "destination": "/app.html" }
  ]
}
